go to streamlit
go to github folder path change branch an dall
select correct python version 'chose dmain
you can add key value pair,:
HF_t="rgrwGW"
 access it using st.secrets["HF_t"]








 --------------------------------
inide .github/workflows/main.yaml
"""

name: Sync to Hugging Face hub
on:
  push:
    branches: [main]

  # to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  sync-to-hub:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
          lfs: true
      - name: Push to hub
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: git push https://HF_USERNAME:$HF_TOKEN@huggingface.co/spaces/HF_USERNAME/SPACE_NAME main

"""





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



 create space in hf get optn to clone

 in files push from github to its files
 in acess token create a token type--write..token named token1

 copy token

 in run.... replace ib git push.... HF_USERNAME, HF_USERSPACE, 

 in github setiigs secret and variable, create repository secret key,it should be named HF_TOKEN

 commmit changes in github with yaml




later i had to use


name: Sync to Hugging Face hub
on:
  push:
    branches: [main]
  workflow_dispatch:
#sdf
jobs:
  sync-to-hub:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
          lfs: true

      - name: Add Hugging Face remote
        run: |
          git remote add space https://huggingface.co/spaces/spearb0lt/langchain-search-bot-from-github

      - name: Push to hub
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          git push --force https://spearb0lt:$HF_TOKEN@huggingface.co/spaces/spearb0lt/langchain-search-bot-from-github main

also its mandortory to upload readme
